---
quick_id: 260903-fao
title: "CI (GitHub Actions) + clear lint debt to green"
date: 2026-09-03
status: complete
---

# Quick Task 260903-fao — Summary

## What was done

Stood up GitHub Actions CI and cleared all outstanding lint debt so the
lint gate is green on the first run.

### Commits (atomic, code only)
1. `534b8e8` fix(backend): clear ruff findings for green lint gate
2. `1132560` fix(frontend): clear ESLint findings for green lint gate
3. `e14c8b7` ci: add GitHub Actions workflow (backend + frontend)

### 1. CI workflow — `.github/workflows/ci.yml`
- Triggers: push to `main` + all PRs; `concurrency` cancels superseded runs.
- **Backend job**: `ruff check` → `alembic upgrade head` → `pytest`, against a
  `postgres:16` **service container** with a `pg_isready` healthcheck.
  uv-managed; `UV_PYTHON=3.12` pins Python to match `Dockerfile`
  (`python:3.12-slim`) and ruff `target-version`. `DATABASE_URL` points at the
  throwaway service — **never** Railway prod.
- **Frontend job**: pnpm pinned to `11.18.0`, `pnpm install --frozen-lockfile`
  (esbuild build already approved in `pnpm-workspace.yaml`), then
  `pnpm run lint` (eslint) + `pnpm run test` (vitest). Node 20 + pnpm cache.

### 2. Backend ruff — cleared (5 findings, not 3)
TECH-DEBT §3 listed 3; the tree actually had 5. All cleared:
- `invitations.py:10` — removed unused `sqlalchemy.func` import.
- `invitations.py:93`, `invitations.py:256`, `cleanup.py:87` — wrapped E501
  long lines (behavior unchanged).
- Plus an autogenerated `alembic/versions/…:31` E501: excluded
  `alembic/versions` from ruff (`extend-exclude`) so future
  `alembic revision --autogenerate` runs don't keep re-reddening the gate.
  Hand-written alembic files (`env.py`) stay linted.

### 3. Frontend ESLint — cleared (3 findings)
- `AuthCallbackPage.jsx` — removed unused `error` var (`no-unused-vars`).
- `SparkleTrail.jsx` — moved `spawnSparkles` into new
  `components/recipient/sparkles.js`; the `.jsx` now only exports a component
  (`react-refresh/only-export-components`).
- `AuthContext.jsx` — moved the `AuthContext` object + `useAuth` hook into new
  `context/auth-context.js`; the `.jsx` now only exports `AuthProvider`.
  Updated the 3 `useAuth` importers (ProtectedRoute, DashboardPage,
  LandingPage). `App.jsx` (`AuthProvider` import) unchanged.

## Verification (local)
- `ruff check .` → **All checks passed!**
- `pnpm run lint` → **0 problems**; `pnpm run build` → success (no broken
  imports); `pnpm run test` → **9 passed**.
- **Full CI backend recipe** exercised against a throwaway podman
  `postgres:16` on Python **3.12**: `alembic upgrade head` clean →
  `pytest` = **27 passed, 1 failed**.
  - The 1 failure is `test_auth.py::test_login_redirects_to_google`, which
    makes a real outbound HTTPS call to fetch Google's OIDC discovery doc.
    It fails **only** here due to the corporate proxy's SSL interception
    (`CERTIFICATE_VERIFY_FAILED`). On clean GitHub-hosted runners this fetch
    succeeds — matches the known "~28 tests green" baseline. Not a regression
    (my changes were lint-only, no behavior change).

## Notes / follow-ups
- **Local `.python-version` drift**: it says `3.13` while prod/Dockerfile/ruff
  target `3.12`. CI overrides via `UV_PYTHON=3.12`; the stray file is
  untouched (out of scope) but worth aligning later.
- `test_login_redirects_to_google` is network-dependent (real Google fetch).
  It passes on CI runners but is inherently fragile — consider mocking the
  OIDC discovery in a future hardening pass.
- `spawnSparkles`/`SparkleTrail` currently have no importers in `src` (possible
  dead code from Phase 3). Preserved as-is — removal is out of scope.
