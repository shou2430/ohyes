## ISSUES FOUND

**Phase:** 04-notifications-invitation-lifecycle
**Plans checked:** 04-01, 04-02, 04-03, 04-04
**Issues:** 0 blocker(s), 4 warning(s)

### Summary of hard checks (all passed)

- Requirement coverage: NOTF-01/02/03, INV-07 all present in `requirements:` frontmatter across plans, with concrete covering tasks. No gap.
- Wave-2 file ownership: 04-03 and 04-04 `files_modified` have zero overlap (verified by inspection; also enforced by each plan's own `git diff --name-only` acceptance criterion).
- `main.py` is written by 04-02 (wave 1) and again by 04-04 (wave 2, `depends_on: [04-02]`) — genuinely sequential, not concurrent. 04-04 explicitly asserts `include_router(notifications_router)` survives (line grep check), confirming it edits on top of 04-02's version rather than racing it.
- Tracer contract: 04-02's `<objective>` states explicitly "04-03 ... and 04-04 ... MUST NOT start until this plan's end-to-end verification passes," and both 04-03/04-04 carry `depends_on: [04-02]`. Gate is real, not aspirational prose only.
- Reversibility gate: 04-04 is `autonomous: false`; Task 2 is `checkpoint:decision gate="blocking"` with `reversibility rating="one-way"`, positioned immediately before Task 3, which is the only task that implements the destructive notification-retention delete (D-07). Task 1 (invitation sweep, rated "costly" not "one-way") correctly precedes the gate since it is not the one-way D-07 concern. Correct positioning.
- Spec-less probe fallback, 5 rows (NOTF-01 unclassified, NOTF-02 empty, NOTF-02 encoding, NOTF-03 unclassified, INV-07 unclassified): all 5 are accounted for — 04-02 authors NOTF-02×2 as covered `must_haves.truths` and explicitly flags NOTF-01 as unresolved assumption A-02-1 (not silently dropped); 04-03 authors NOTF-03 as a covered truth citing D-08/D-09/D-10; 04-04 authors INV-07 as a covered truth citing D-20/D-21/D-22. No-silent-drop equality holds.
- Backstop marker (E3 long-text, 04-02 line 55-56): correctly shaped as `statement:` + sibling `verification: backstop` — flat scalar, no nesting, no prose-only note.
- Prohibitions: no plan fabricates `check_kind`/`check_target`/`check_rule`/`check_violation_fixture`/`check_clean_fixture` descriptor keys anywhere. Compliant (see Warning 4 below for a related style-only note).
- UI-SPEC lift: 04-02's `must_haves.truths` contains exactly the 24 covered E1-E5 rows + 1 backstop + 1 cross-cutting row (counted: E1=5, E2=6, E3=3+1 backstop, E4=3, E5=7, cross-cutting=1 → 24 covered + 1 backstop + 1 cross-cutting = 26 total, matching the declared 24+1+1 breakdown). 04-03 explicitly states it does not duplicate this lift. Nothing dropped between them.
- Decision coverage: D-01 through D-23 all traced to at least one plan (spot-checked every ID; all present in `must_haves`, task actions, or threat-model text).
- Threat models: T-04-IDOR present and mitigated with a real test in both 04-02 (GET) and 04-03 (POST /read). T-04-SILENT and T-04-LOCKKEY are transferred from 04-01 to 04-04 and genuinely mitigated there (distinctive lock key constant + mandatory log line + dedicated tests).
- Dependency graph: 04-01 (wave0, no deps) → 04-02 (wave1, deps [04-01]) → {04-03, 04-04} (wave2, deps [04-02]). Acyclic, consistent wave numbers.
- Key links: all `must_haves.key_links` in 04-02/04-03/04-04 map onto concrete task actions (router registration, IDOR filter, state-flow chain, lifespan wiring, DELETE...RETURNING → file path). None float free of an implementing task.
- Context compliance: no scope-reduction language ("v1 static", "future enhancement", "not wired to", "stub" used to justify omission) found gating a locked decision. D-06 (no pagination) and D-15 (no loading state) are actively enforced via negative grep acceptance criteria rather than silently narrowed.
- No forbidden CLAUDE.md libraries (Celery/Redis/WebSockets/Redux/styled-components/SQLModel/PgBouncer) appear; APScheduler addition is correctly gated by 04-01's blocking human legitimacy checkpoint before install.
- Nyquist (Dimension 8): VALIDATION.md exists; every `auto`/`tdd` task across all 4 plans carries an `<automated>` verify command; no watch-mode flags; no full E2E-suite-as-unit-test; no 3-consecutive-task gap without automated verify; no unresolved `MISSING` verify markers. PASS.
- Verify-command format sanity: no `pnpm ls | grep '^package'` anchor pattern, no `2>/dev/null || echo` swallowed-error-feeds-comparison pattern found in any `<automated>` block.

### Warnings (should fix, do not block execution)

**1. [scope_reduction / decision_deviation — informational, not a reduction] 04-04 deviates from D-19's literal function name**
- Plan: 04-04
- D-19 names `pg_try_advisory_lock` (session-scoped); 04-04's `## Decision deviations` section documents using `pg_try_advisory_xact_lock` (transaction-scoped) instead, citing D-19's own stated intent ("local to the job function") and RESEARCH Pitfall 1 (pooled-connection stranding) as justification.
- This is a scope *increase in correctness*, not a reduction — flagging only because any literal-decision-text deviation deserves an explicit reviewer nod. No fix required; the plan already discloses this transparently and it does not reduce delivered behavior.

**2. [scope_sanity] 04-01 and 04-04 each have 5 tasks, at the literal "5+ = blocker, split required" threshold**
- Plans: 04-01 (5 tasks: 4 auto/tdd + 1 `checkpoint:human-verify` gate), 04-04 (5 tasks: 4 auto/tdd + 1 `checkpoint:decision` gate)
- The scope-sanity table's raw threshold is crossed by task count, but in both cases one of the five is a blocking human checkpoint that consumes minimal executor context (present info, wait for input) rather than a full implementation task. Treating it as a WARNING rather than a hard blocker because splitting either plan around its checkpoint would fragment a task that is already gated and sequential by nature (e.g. splitting 04-04 at the D-07 checkpoint would just create a 3-plan wave-2 chain with no material benefit).
- Fix (optional): if executor context pressure is observed in practice, split 04-04 into "invitation sweep" (Task 1) / "notification retention + scheduler wiring" (Tasks 2-4) / "requirements doc update" (Task 5).

**3. [task_completeness] 04-02's `files_modified` list is 10 files — at the "10 = warning" scope-sanity threshold**
- Plan: 04-02
- This is the declared tracer plan and is deliberately thick by design (touches every layer once) — the plan's own objective explains why. Not recommending a split since a tracer's entire value is touching every layer in one commit, but flagging since it sits exactly at the warning line.

**4. [task_completeness / consistency] Inconsistent `must_haves.prohibitions` shape across plans**
- Plans: 04-01/04-02 use structured objects (`statement` + `category` + `requirement_id` + `verification: judgment`); 04-03/04-04 use plain YAML strings with no metadata.
- Neither shape violates the descriptor-less rule (no `check_*` keys anywhere), so this is not a blocker — but the inconsistency will make automated cross-plan tooling that expects the richer shape silently skip 04-03/04-04's prohibitions. Recommend normalizing 04-03/04-04's prohibitions to the same structured-object shape used in 04-01/04-02 for tooling consistency.

**5. [task_completeness] 04-03 Task 2 and Task 3 lack `<reversibility>` tags**
- Plan: 04-03
- 04-01, 04-02, and 04-04 tag every task (or nearly every task) with `<reversibility rating="...">`; 04-03's Task 1 has one but Tasks 2 and 3 do not. Given `REVERSIBILITY_GATES=true` for this phase and 04-03's own leanness flagged in the verification brief, recommend adding the tag for consistency (both are "reversible" — pure client-side/UI changes — so this is a documentation gap, not a missing gate).

### Recommendation

No blockers found. Plans are internally consistent, requirement coverage is complete, the tracer/wave/checkpoint gating is real and correctly ordered, the spec-less edge probe rows are fully accounted for with no silent drops, and Nyquist compliance passes. The 5 warnings above are quality/consistency nits — safe to proceed to `/gsd-execute-phase 4` as-is, or address warnings 4-5 first if a quick pass is convenient.
