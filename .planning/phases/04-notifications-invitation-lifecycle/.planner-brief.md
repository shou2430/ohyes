# Phase 4 Planner Brief — Notifications & Invitation Lifecycle

<planning_context>
**Phase:** 04
**Phase name:** Notifications & Invitation Lifecycle
**Phase directory:** `/home/jasonliu/projects/ohyes/.planning/phases/04-notifications-invitation-lifecycle`
**Padded phase:** `04`
**Mode:** standard

**Phase Goal (from ROADMAP.md):** Close the creator feedback loop with a notification system and ensure invitations auto-expire with full data cleanup after 7 days.

**Success Criteria (from ROADMAP.md):**
1. Creator sees a red dot/heart indicator on the dashboard when a new notification arrives
2. Notification displays "[Name] said yes to your [title]" with the recipient's optional message
3. Creator can mark notifications as read and the indicator clears
4. An invitation older than 7 days is automatically deleted along with its photo file

**Depends on:** Phase 3

<files_to_read>
- `/home/jasonliu/projects/ohyes/.planning/STATE.md` (Project State)
- `/home/jasonliu/projects/ohyes/.planning/ROADMAP.md` (Roadmap)
- `/home/jasonliu/projects/ohyes/.planning/REQUIREMENTS.md` (Requirements)
- `/home/jasonliu/projects/ohyes/.planning/phases/04-notifications-invitation-lifecycle/04-CONTEXT.md` (USER DECISIONS from /gsd-discuss-phase — 23 locked decisions D-01..D-23. THESE ARE BINDING.)
- `/home/jasonliu/projects/ohyes/.planning/phases/04-notifications-invitation-lifecycle/04-RESEARCH.md` (Technical Research — includes `## Validation Architecture` and `## Code Examples`)
- `/home/jasonliu/projects/ohyes/.planning/phases/04-notifications-invitation-lifecycle/04-PATTERNS.md` (Pattern Map — analog files and code excerpts)
- `/home/jasonliu/projects/ohyes/.planning/phases/04-notifications-invitation-lifecycle/04-VALIDATION.md` (Nyquist validation strategy — Dimension 8)
- `/home/jasonliu/projects/ohyes/.planning/phases/04-notifications-invitation-lifecycle/04-UI-SPEC.md` (UI Design Contract — visual/interaction specs. Lift its `## UI Considerations` section per the rules below.)
- `/home/jasonliu/projects/ohyes/.planning/phases/04-notifications-invitation-lifecycle/.planner-contributions.md` (**MANDATORY** — active capability contributions: ai-integration API-coverage checkpoint, assumption-delta, schema-gate, security threat model. Follow every block in it.)
</files_to_read>

**No AI-SPEC.md and no plain SPEC.md exist for this phase.** (A `04-UI-SPEC.md` does exist — read it.)

**Phase requirement IDs (every ID MUST appear in a plan's `requirements` field):** NOTF-01, NOTF-02, NOTF-03, INV-07

**Project instructions:** Read `./CLAUDE.md` — follow project-specific guidelines (tech stack is locked: React 19 + Vite + Tailwind v4 + Motion 12 + react-i18next on the frontend; Python 3.12 + FastAPI + SQLAlchemy 2.0 async + asyncpg + Alembic + APScheduler on the backend; PostgreSQL 16; Railway).
**Project skills:** Check `.claude/skills/` or `.agents/skills/` if either exists — plans must account for project skill rules.

**Response language:** 繁體中文 for any user-facing prose you emit back to the orchestrator. **PLAN.md file content, code, identifiers, paths, and XML tags stay in English.**

**TRACER_MODE:** true — LEAD with one production-quality end-to-end `type="tracer"` slice that is verified before any expansion task. Do not plan horizontal layers first.
**REVERSIBILITY_GATES:** true — rate each decision's reversibility; a decision rated `one-way` earns a `checkpoint:decision` before the task implementing it. Note: **D-07 is explicitly flagged one-way** in CONTEXT.md (30-day notification deletion destroys recipient messages that exist nowhere else). Follow `$HOME/.claude/gsd-core/references/planner-reversibility.md`.
**MVP_MODE:** false — ignore MVP/vertical-slice guidance entirely.
**WALKING_SKELETON:** false — do not produce SKELETON.md.
**Granularity:** standard
**Security:** ASVS L1, block_on = high. Every PLAN.md needs a `<threat_model>` block.
</planning_context>

<specless_probe_fallback>
**Spec-less probe fallback is ACTIVE.** This phase has no plain `SPEC.md`, so both `## Edge Coverage`
and `## Prohibitions` are absent (`EDGE_ABSENT=1`, `PROHIB_ABSENT=1`). Author their predicates into
`must_haves` per §A/§B/§C of `$HOME/.claude/gsd-core/references/specless-probe-fallback.md`
(descriptor-less prohibitions, never auto-dismiss, no silent drops). Read that reference and, for §B,
`$HOME/.claude/gsd-core/references/prohibition-probe.md`.

**Edge coverage report (`$COVERAGE`, deterministic — regenerated from the 4 phase requirement IDs):**

```json
{
  "items": [
    { "requirement_id": "NOTF-01", "category": "unclassified", "status": "unresolved", "verification": null, "resolution": null, "reason": null, "probe": "unclassified — review manually" },
    { "requirement_id": "NOTF-02", "category": "empty", "status": "unresolved", "verification": null, "resolution": null, "reason": null, "probe": "What is the result for empty, single-element, or null input?" },
    { "requirement_id": "NOTF-02", "category": "encoding", "status": "unresolved", "verification": null, "resolution": null, "reason": null, "probe": "Whose definition of length/equality applies — bytes, code points, grapheme clusters, or normalized form?" },
    { "requirement_id": "NOTF-03", "category": "unclassified", "status": "unresolved", "verification": null, "resolution": null, "reason": null, "probe": "unclassified — review manually" },
    { "requirement_id": "INV-07", "category": "unclassified", "status": "unresolved", "verification": null, "resolution": null, "reason": null, "probe": "unclassified — review manually" }
  ],
  "coverage": { "applicable": 5, "resolved": 0, "unresolved": 5, "byVerification": { "explicit": 0, "backstop": 0 } }
}
```

All 5 rows are `unresolved` (4 of them `unclassified`). Per §A, an `unclassified` row stays
`unresolved` — it is **never** auto-`backstop`ped. Per §C, every `unresolved` row must surface as an
**explicit flagged planner assumption** in the owning plan (never a silent drop). No-silent-drop
equality holds when: 5 probe-surfaced edges == (# authored into `must_haves` + # surfaced as flagged
assumptions).

Note that `04-CONTEXT.md` and `04-RESEARCH.md` between them already answer several of these probes
(e.g. NOTF-02's empty/null case is D-03's optional-message handling; the encoding question is the
30-char message limit from CLAUDE.md). Where a locked decision defensibly answers a probe, you MAY
author it as a `covered` acceptance criterion in `must_haves.truths` and cite the D-NN — that is not
auto-resolution, it is a documented resolution with a source. Anything not answered stays a flagged
assumption.
</specless_probe_fallback>

<downstream_consumer>
Output consumed by `/gsd-execute-phase`. Plans need:
- Frontmatter (wave, depends_on, files_modified, autonomous, requirements)
- Tasks in XML format with `<read_first>` and `<acceptance_criteria>` fields (MANDATORY on every task)
- Verification criteria
- `must_haves` for goal-backward verification
- **Edge coverage (fallback source `$COVERAGE` above, since no SPEC section exists):** every `covered`
  edge's acceptance criterion → `must_haves.truths` as a plain string; every `backstop` edge → a
  **structured flat-scalar marker** `{ statement: <the check>, verification: backstop }` in
  `must_haves.truths`, NOT a prose note (the verifier branches deterministically on the
  `verification: backstop` field; a parenthetical is unparseable). Use a flat scalar `verification:`
  continuation key, never a nested object. `unresolved` edges are explicit assumptions — surface them
  in the plan, do not silently drop them.
- **Prohibitions (fallback source, since no SPEC section exists):** run the §B recall pass and author
  each kept prohibition into the `must_haves.prohibitions:` sibling block (NOT `truths`) via the single
  `projectProhibitions` serializer, **descriptor-less** (no `check_kind` / `check_target` /
  `check_rule` / `check_violation_fixture` / `check_clean_fixture`) so each disposes flagged-unverified.
  Never auto-dismiss. Drop canon security/compliance items with a one-line breadcrumb.
- **`04-UI-SPEC.md` `## UI Considerations`:** if that section exists, lift it by the **identical rule**
  as `## Edge Coverage` — `covered` → `must_haves.truths` string, `backstop` → flat scalar
  `{ statement, verification: backstop }`, `unresolved` → explicit planner assumption.
- **"Artifacts this phase produces" section (MANDATORY)** — list every symbol this phase creates:
  classes, functions, API endpoint paths, React components/hooks, config keys, dependency additions,
  new file paths, Alembic revision ids (if any).
</downstream_consumer>

<deep_work_rules>
## Anti-Shallow Execution Rules (MANDATORY)

Every task MUST include these fields — they are NOT optional:

1. **`<read_first>`** — Files the executor MUST read before touching anything. Always include:
   - The file being modified (so executor sees current state, not assumptions)
   - Any "source of truth" file referenced in CONTEXT.md (reference implementations, existing patterns, config files, schemas)
   - Any file whose patterns, signatures, types, or conventions must be replicated or respected

2. **`<acceptance_criteria>`** — Verifiable conditions that prove the task was done correctly. Rules:
   - Every criterion must be checkable as a source assertion, behavior assertion, test command, or CLI output
   - NEVER use subjective language ("looks correct", "properly configured", "consistent with")
   - Include exact strings, patterns, values, command outputs, or observable behavior where that is the right proof
   - Examples:
     - Code: `notifications.py contains async def mark_all_read(` / `pytest backend/tests/test_notifications.py exits 0`
     - Behavior: `GET /api/notifications returns 200 + newest-first list for an authenticated creator`
     - Config: `backend/pyproject.toml contains apscheduler` / `main.py lifespan starts AsyncIOScheduler`
     - Docs: `README.md contains '## Scheduled Jobs'`

3. **`<action>`** — Must include CONCRETE values, not references. Rules:
   - NEVER say "align X with Y", "match X to Y", "update to be consistent" without specifying the exact target state
   - Include concrete identifiers and reference values: config keys, function signatures, SQL table names, class names, import paths, env vars, endpoint paths, etc.
   - If CONTEXT.md has a comparison table or expected values, copy only the target identifiers/values needed to remove ambiguity
   - Do not include full file contents, fenced code blocks, or complete implementations in `<action>`
   - The executor should understand the intended target state from `<action>` and use `<read_first>` files for current implementation details, patterns, and source-of-truth context

**Why this matters:** Executor agents work from the plan text. Vague instructions like "update the config to match production" produce shallow one-line changes. Concrete instructions like "add DATABASE_URL, set POOL_SIZE=20, add REDIS_URL, and read config/runtime.ts before editing" produce complete work without turning the planner into the executor.
</deep_work_rules>

<quality_gate>
- [ ] PLAN.md files created in phase directory
- [ ] Each plan has valid frontmatter
- [ ] Tasks are specific and actionable
- [ ] Every task has `<read_first>` with at least the file being modified
- [ ] Every task has `<acceptance_criteria>` with behavior, test-command, CLI, or source assertions
- [ ] Every `<action>` contains concrete identifiers without fenced code blocks or full implementations
- [ ] Dependencies correctly identified
- [ ] Waves assigned for parallel execution
- [ ] `must_haves` derived from phase goal
- [ ] Every PLAN.md includes an "Artifacts this phase produces" section
- [ ] Every PLAN.md includes a `<threat_model>` block (ASVS L1, block_on=high)
- [ ] All 5 probe-surfaced edges represented in `must_haves` or as flagged assumptions (no silent drops)
- [ ] Every UI-SPEC `## UI Considerations` covered/backstop item represented in `must_haves` (no silent drops)
- [ ] Kept prohibitions authored descriptor-less into `must_haves.prohibitions`
- [ ] **Every trackable decision D-01..D-23 from 04-CONTEXT.md is cited by at least one plan** (a blocking gate runs after planning — an uncited D-NN fails the phase)
- [ ] All 4 requirement IDs (NOTF-01, NOTF-02, NOTF-03, INV-07) appear in some plan's `requirements` field
</quality_gate>
