---
context: phase
phase: 04-notifications-invitation-lifecycle
task: 8
total_tasks: 15
status: in_progress
last_updated: 2026-08-02T15:03:40.441Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **Slow API gateway kills long agent runs** — this environment routes through a corporate Anthropic gateway at ~76 output tokens/sec (measured: a max-effort Opus 5 request producing 32k output tokens took 423s). Two consecutive `gsd-phase-researcher` spawns on opus died with `Agent terminated early due to an API error: The operation timed out` and wrote nothing to disk. — **Structural mitigation:** `workflow.plan_chunked` is now `true`. The planner MUST run in chunked mode (short outline run, then one short run per plan, each committed individually). Never spawn a single long-lived opus planner here.

- [ ] CONSTRAINT: **An empty Agent return does not mean the agent failed** — both the sonnet researcher and the sonnet pattern-mapper returned nothing to the orchestrator while having written complete, correct files to disk. — **Structural mitigation:** always `ls` / `wc` the expected output path before treating an empty return as a failure. The GSD workflows have filesystem-fallback steps (plan-phase 9a and 11a) for exactly this; use them rather than re-spawning and duplicating work.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Spawning opus subagents for long tasks | On this gateway an opus subagent doing deep thinking + file reads + writes exceeds the client request timeout before returning. Observed twice this session, zero disk output both times. | blocking | Chunked mode for the planner (`workflow.plan_chunked=true`, already set). Sonnet for non-critical agents. The Agent tool has no per-spawn `effort` parameter — `gsd-phase-researcher.md` frontmatter already declares `effort: high` and it still timed out — so `model` is the only lever available at spawn time. |
| Assuming multiple backend replicas | The Railway persistent volume holding `/data/photos` can be mounted by only one active deployment. Any plan that assumes 2+ backend replicas in this milestone silently breaks photo upload (`invitations.py:130`) and photo serving (`routers/photos.py`) on the replicas without the mount — taking down already-shipped Phase 2 and Phase 3 features. | advisory | Phase 4 plans must target a single backend container. The scheduler's advisory lock (D-19) is forward-looking insurance, not permission to scale. Scaling is gated on INFR-V2-01 (next milestone). |
| Re-litigating the scheduler choice | APScheduler vs Celery/Redis was compared twice and settled both times. The deciding factor is the volume constraint, not preference: a separate worker service cannot mount the Railway volume and so could not delete photo files, failing INV-07 outright. | advisory | Read `04-DISCUSSION-LOG.md` § Expiry Cleanup before reopening. The comparison table and the volume argument are recorded there in full. |

<current_state>
Paused mid-`/gsd-plan-phase 4`, between step 7.95 and step 8. This is a paused **planning** workflow — no Phase 4 code exists and **no PLAN.md files have been written yet**.

Steps 1 through 7.95 are complete and every artifact is committed. Working tree has one uncommitted file (`.planning/config.json`, the `plan_chunked` flip) which this handoff commit includes.

Phases 1-3 are complete and deployed. Phase 4 is the second-to-last phase of milestone v1.
</current_state>

<completed_work>

Completed this session (all `/gsd-plan-phase 4` orchestration steps):

- Step 1-3: init, closed-phase gate (status `Pending`, not gated), phase validated against ROADMAP. Resolved `MVP_MODE=false`, `WALKING_SKELETON=false`, `TRACER_MODE=true`, `REVERSIBILITY_GATES=true`, `granularity=standard`, `CHUNKED_MODE` now true.
- Step 4: loaded pre-existing `04-CONTEXT.md` (23 locked decisions D-01..D-23).
- Step 5: **`04-RESEARCH.md`** written — 576 lines, committed `5f71c04`. Includes the `## Validation Architecture` section Nyquist requires, an `## Architectural Responsibility Map`, 5 concrete implementation patterns, 5 pitfalls, and an honest Sources section marking which claims are unverified training knowledge.
- Step 5.5: **`04-VALIDATION.md`** written from the template + research — committed `9f80dda`. Status `draft`, `nyquist_compliant: false`, Wave 0 gaps listed.
- Step 5.55: security threat model gate active — ASVS **L1**, `block_on: high`. Every PLAN.md must carry a `<threat_model>` block.
- Step 5.6: UI gate → Branch 3, `04-UI-SPEC.md` found and will be passed to the planner as the design contract. AI-integration hook skipped (no AI keywords in the phase goal).
- Step 5.65: codebase-drift precheck → `skipped` (`no-structure-md`), no action required.
- Step 6: no existing plans.
- Step 7.8: **`04-PATTERNS.md`** written — 244 lines, committed `94cbcab`. Maps each new/modified file to its closest existing analog with code excerpts.
- Step 7.9: intel hook inactive → `API_SURFACE_PATH` empty.
- Step 7.95: spec-less probe fallback ran (no `04-SPEC.md` exists, so `EDGE_ABSENT=1` and `PROHIB_ABSENT=1`). Deterministic edge probe returned **5 applicable edges, all `unresolved`**: NOTF-01 unclassified, NOTF-02 empty-input, NOTF-02 encoding/length-definition, NOTF-03 unclassified, INV-07 unclassified.

Also this session: diagnosed the API timeouts (see BLOCKING CONSTRAINTS) and set `workflow.plan_chunked=true`.
</completed_work>

<remaining_work>

- **Step 8.5 — chunked planner run.** Outline agent first (writes `04-PLAN-OUTLINE.md`, returns `## OUTLINE COMPLETE`), then one short agent per plan, committing each. Resume-safe: a plan file that already exists with valid frontmatter is skipped.
- **Step 9 / 9a** — handle planner return; expect a possibly-empty return and use the filesystem fallback.
- **Step 10-12** — `gsd-plan-checker` verification and the revision loop (max 3 iterations, stall detection on non-decreasing issue count).
- **Step 13** — requirements coverage gate: NOTF-01, NOTF-02, NOTF-03, INV-07 must each appear in some plan's `requirements` field.
- **Step 13a** — decision coverage gate. **This one blocks and exits non-zero.** Every trackable decision in `04-CONTEXT.md` `<decisions>` must be cited by at least one plan.
- **Step 13b-13e** — STATE.md update, ROADMAP wave-dependency annotation, commit plans, post-planning gap analysis.
- Two `REQUIREMENTS.md` updates the planner must make: reclassify **NOTF-V2-02** (30-day notification retention) from v2 to a v1 requirement mapped to Phase 4, and add it to the traceability table.
</remaining_work>

<decisions_made>

Phase-4 design decisions are all in `04-CONTEXT.md` (D-01 to D-23) and unchanged this session. Session decisions:

- **Planner runs opus + chunked mode.** Best quality that survives this gateway; each plan commits individually so a crash resumes from the last good plan. Recorded as `workflow.plan_chunked=true`.
- **Researcher and pattern-mapper were run on sonnet**, deviating from the configured `researcher_model: opus`, after two opus timeouts. Config itself was not changed — this was a per-spawn override for this session.
- **The API failures are gateway throughput, not proxy misconfiguration.** `NO_PROXY` correctly covers `.wneweb.com.tw` so the API host bypasses the squid proxy at `172.16.100.51:3128`; `connect=0.128s`; SSE passes through unbuffered (TTFB 0.32-0.43s), which rules out the common Nginx `proxy_buffering` fault. The gateway is simply slow.

The design decisions from `04-CONTEXT.md` that shape the plans most:

- Top bar heart icon + red dot → dropdown panel. No count badge, no dedicated route.
- Opening the panel marks everything read in one call; mark-read failures fail silently.
- Dashboard polls every 30 seconds; heart does a one-shot bounce when the dot appears. 401 logs out; network errors retry silently.
- Notifications auto-delete after 30 days (NOTF-V2-02 pulled forward). Rated **one-way** — recipient messages exist nowhere else once the invitation is destroyed on respond. Under default `REVERSIBILITY_GATES=true` this earns a `checkpoint:decision` before the implementing task.
- APScheduler in the FastAPI lifespan, hourly, guarded by `pg_try_advisory_lock` (transaction-scoped, not session-scoped — see RESEARCH Pitfall 1).
- v1 stays single-container with the volume.
- Expired invitations vanish silently from the dashboard; photo deletion failures are logged and the DB row is deleted regardless.

The `Notification` model already exists from Phase 3 (`backend/app/models/notification.py`) with every field needed. No migration is required for the notification feature itself — only for adding `apscheduler` to `backend/pyproject.toml`.
</decisions_made>

<blockers>
- **Slow gateway / opus subagent timeouts** — see BLOCKING CONSTRAINTS. Mitigated in-session by chunked mode; optionally mitigated permanently by setting `API_TIMEOUT_MS` and/or lowering `CLAUDE_EFFORT` (neither changed — both need a session restart).
- **WR-007** (carried from Phase 3): `test_verify_correct_password` and `test_respond_creates_notification` are empty stubs — `03-01-SUMMARY.md:14`. `04-RESEARCH.md` § Code Examples has the `db_session` transaction-rollback fixture that unblocks them, and `04-VALIDATION.md` Wave 0 already lists unstubbing them.
- **Limited network egress.** `WebSearch` is blocked by org policy for this model. `WebFetch` fails against `docs.claude.com` and `docs.railway.com` (http=000 / socket closed) but succeeds against the PyPI JSON API — which is how `apscheduler 3.11.3` was version-verified. RESEARCH Open Question 1 (APScheduler coroutine-job behavior) and the advisory-lock scoping claim remain unverified against official docs; Wave 0 includes a smoke test to settle Open Question 1 empirically.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured state; the `blockers` and `next_action` fields are the fastest way back in.
2. `.planning/phases/04-notifications-invitation-lifecycle/04-CONTEXT.md` — the 23 locked decisions. Do not re-ask any of them.
3. `.planning/phases/04-notifications-invitation-lifecycle/04-RESEARCH.md` — implementation patterns, pitfalls, and the Validation Architecture. Read § Architectural Responsibility Map and § Recommended Project Structure first.
4. `.planning/phases/04-notifications-invitation-lifecycle/04-PATTERNS.md` — closest existing analog for each file the phase creates or modifies.
5. `.planning/phases/04-notifications-invitation-lifecycle/04-UI-SPEC.md` — approved design contract (~48KB; read selectively). Its `## UI Considerations` section must be lifted into `must_haves` by the same rule as a SPEC `## Edge Coverage`.
6. `.planning/REQUIREMENTS.md` — NOTF-01/02/03 and INV-07 acceptance criteria; NOTF-V2-02 (to be reclassified into v1/Phase 4); INFR-V2-01 (next-milestone storage migration).
7. `CLAUDE.md` — stack constraints, especially the Infrastructure section on Railway volume mounting.
8. `.planning/phases/04-notifications-invitation-lifecycle/04-DISCUSSION-LOG.md` — only if a decision needs reopening; it records what was rejected and why.

## Infrastructure State

- No background processes, dev servers, or watchers were started this session.
- Branch `main`. Clean apart from `.planning/config.json`, committed with this handoff.
- Deployed on Railway: frontend static site, backend Docker container with the persistent volume at `/data/photos`, PostgreSQL plugin. Single backend replica — by constraint, and by explicit decision for this milestone.
- `apscheduler` is NOT yet in `backend/pyproject.toml`. Phase 4 adds it (`cd backend && uv add apscheduler`; 3.11.3 verified live on PyPI 2026-06-28).
- API routes through `ANTHROPIC_BASE_URL=http://orion-model.wneweb.com.tw/anthropic/` (Vertex-backed, ~76 tok/s). `CLAUDE_EFFORT=max` is set in the environment and overrides `effortLevel: "high"` in `~/.claude/settings.json`. `API_TIMEOUT_MS` is unset.
- GSD config now has `workflow.plan_chunked: true`; `planner_model` remains `opus`.

<context>
The plan was to run `/gsd-plan-phase 4` straight through. Everything up to the planner spawn went fine, but the research step burned most of the session: two opus `gsd-phase-researcher` spawns timed out with zero output, and rather than blindly retrying, the user asked what was actually causing the API failures and whether their proxy was misconfigured.

It was not the proxy. Direct measurement showed the gateway is reachable in 0.128s, returns 200, and streams SSE without buffering — but only pushes ~76 output tokens/sec, so a single max-effort Opus 5 request can run seven minutes. That is what was killing the subagents, and it is why the researcher and pattern-mapper were re-run on sonnet (both succeeded, both returned empty while writing correct files to disk).

The user then chose opus + chunked mode for the planner and asked to pause here. The decision is recorded in three places so it cannot be lost: `workflow.plan_chunked=true` in config (operative), this handoff, and the user's project memory.

Nothing is half-finished. The only in-memory artifact not on disk is the step-7.95 edge-probe report, and that is deterministic — it regenerates identically from the same four requirement IDs.
</context>

<next_action>
Start with: `/gsd-plan-phase 4`

It will find `has_research=true` and `has_context=true`, skip the research prompt, re-run the cheap deterministic gates, and enter **Step 8.5 chunked planning** automatically because `workflow.plan_chunked` is now `true`. Confirm it reports chunked mode before letting it proceed — if it tries to spawn a single long-lived opus planner, stop it.

The edge-probe report for step 7.95 regenerates from these four requirements: NOTF-01, NOTF-02, NOTF-03, INV-07. Expect 5 applicable edges, all `unresolved` — they must reach the planner as flagged assumptions, not be silently dropped.
</next_action>
